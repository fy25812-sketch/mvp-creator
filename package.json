{
  "name": "dsh-mvp-creator",
  "version": "0.1.0",
  "private": true,
  "description": "Turn one rough idea into a running, deployable MVP with minimal human interaction: /mvp command, deterministic FastAPI+SQLite scaffolding, self-hosted Docker/nginx/HTTPS deploy artifacts, and a Token-per-turn efficiency measurement.",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "files": [
    "src",
    "skills",
    "cordis.patch.yml",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/",
    "test:inline": "node --test --test-isolation=none tests/*.test.js"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "license": "MIT"
}
